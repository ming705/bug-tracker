pipeline {
    agent {
        docker {
            image 'node:25.8.1-alpine'
            reuseNode true
        }
    }

    stages {
        stage('Unit Tests - Frontend') {
            steps {
                dir('bugtracker-frontend') {
                    sh '''
                      npm ci
                      npm test
                    '''
                }
            }
        }
    }
}