pipeline {
    agent {
        docker {
            image 'bug-tracker-backend:latest'
            reuseNode true
        }
    }

    stages {
        stage('Unit Tests - Backend') {
            steps {
                dir('bugtracker-backend') {
                    sh 'go test -v ./...'
                }
            }
        }
    }
}