pipeline {
    agent {
        docker {
            image 'golang:1.26.1-alpine'
            reuseNode true
        }
    }

    stages {
        stage('Unit Tests - Backend') {
            steps {
                dir('bugtracker-backend') {
                    sh 'go test -v ./...'
                }
            }
        }
    }
}